# ── Node ──
NODE_ENV=development
PORT=3000

# ── PostgreSQL ──
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456
POSTGRES_DB=acme
POSTGRES_PORT=5431

GROQ_API_KEY=gsk_

# ── Database (Prisma + API) ──
DATABASE_URL=postgresql://postgres:123456@localhost:5431/acme

# ── CORS / URLs ──
URL_FRONTEND=http://localhost:5173



# ── Vite (client-side, prefixo VITE_ obrigatório) ──
VITE_API_URL=http://localhost:3000

# ── Health Check ──
MEMORY_HEAP_THRESHOLD_BYTES=314572800
DISK_STORAGE_THRESHOLD_PERCENT=0.8
