# ── Node ──
NODE_ENV=development
PORT=3000

# ── PostgreSQL ──
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=acme
POSTGRES_PORT=5432

# ── Database (Prisma + API) ──
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/acme?schema=public"

# ── CORS / URLs ──
URL_API=http://localhost:3000
URL_WEB=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# ── Health Check ──
MEMORY_HEAP_THRESHOLD_BYTES=314572800
DISK_STORAGE_THRESHOLD_PERCENT=0.8
