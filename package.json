{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "name": "acme-wallet",
  "type": "module",
  "packageManager": "pnpm@10.29.2",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "format": "prettier --write .",
    "db:studio": "pnpm --filter @repo/db db:studio",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean && rm -rf node_modules .turbo && pnpm install && pnpm build",
    "prepare": "husky",
    "test:integration": "pnpm  --filter api test:integration"
  },
  "lint-staged": {
    "**/*.{ts,tsx}": "eslint --fix"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.4.1",
    "@commitlint/config-conventional": "^20.4.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "turbo": "^2.8.3",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "overrides": {
      "@types/node": "^22.10.7"
    }
  }
}
